REACT_APP_API_URL=/api
# En desarrollo usamos el proxy del dev server (package.json proxy) para evitar CORS.
# Cuando despliegues, cambia esta variable a la URL completa del backend (ej: https://api.tudominio.com/api)
PORT=3000